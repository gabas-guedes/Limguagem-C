#include <stdio.h>
#include <string.h>

void main()
{
    char linha[200];

    for (int i = 0; i < strlen(linha); i++)
    {
        if (linha[i] == ';')
        {
            // Faz alguma coisa
        }
    }
}
